# Docs

This folder is reserved for product and technical notes as the scaffold grows.

Current architecture constraints:

- modular monolith only
- timeline is a read model, not a standalone module
- messaging is outbound-only in the MVP
- AI remains assistive and always requires human confirmation
